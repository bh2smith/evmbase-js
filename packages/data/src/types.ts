export type Nft = {
  lastTransferredAt: string;
  tokenId: string;
  contractId: string;
  baseUri: string;
  // metadataId: string;
  title: string;
  minter: string;
  // media: string;
  // document: string;
  // animationUrl: string;
};
